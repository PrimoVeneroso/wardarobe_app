import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.armadio.app',
  appName: 'App Guardaroba',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
